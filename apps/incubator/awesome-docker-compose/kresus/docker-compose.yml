services:
  kresus:
    image: bnjbvr/kresus:latest
    container_name: kresus
    ports:
      - "9876:9876"
    volumes:
      - ./config:/home/user/config
      - ./data:/home/user/data
    environment:
      - TZ=${TZ}
    restart: unless-stopped
