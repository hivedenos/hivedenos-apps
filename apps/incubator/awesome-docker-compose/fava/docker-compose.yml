services:
  fava:
    image: yegle/fava:latest
    container_name: fava
    ports:
      - "5000:5000"
    environment:
      - BEANCOUNT_FILE=/data/main.bean
    volumes:
      - ./data:/data
    restart: unless-stopped
