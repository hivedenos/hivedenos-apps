services:
  overseerr:
    image: ghcr.io/seerr-team/seerr:latest
    container_name: overseerr
    environment:
      - LOG_LEVEL=info
      - TZ=Etc/UTC
    ports:
      - 5055:5055
    volumes:
      - ./config:/app/config
    restart: unless-stopped
