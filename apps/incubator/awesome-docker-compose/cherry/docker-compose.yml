version: '3.3'
 
services:
  cherry:
    image: haishanh/cherry:latest
    container_name: cherry
    volumes:
      - ./data:/data
    ports:
      - 
    environment:
      - JWT_SECRET=cherry
      - ENABLE_PUBLIC_REGISTRATION=1
      - USE_INSECURE_COOKIE=1
    restart: always
