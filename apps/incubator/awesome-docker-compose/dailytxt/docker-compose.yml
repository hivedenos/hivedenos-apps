services:
  dailytxt:
    image: phitux/dailytxt:latest
    container_name: dailytxt
    ports:
      - 8080:8080
    volumes:
      - ./data:/app/data
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - SECRET_KEY=${SECRET_KEY}
    restart: unless-stopped
