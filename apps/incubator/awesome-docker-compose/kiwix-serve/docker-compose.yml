services:
  kiwix-serve:
    image: ghcr.io/kiwix/kiwix-tools:latest
    container_name: kiwix-serve
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    command: "*.zim"
    restart: unless-stopped
