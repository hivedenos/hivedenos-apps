version: '3.9'
services:
    vigil:
        image: 'valeriansaliou/vigil:v1.26.3'
        volumes:
            - './config.cfg:/etc/vigil.cfg'
        ports:
            - '8080:8080'
        restart: always
