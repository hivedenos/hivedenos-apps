services:
  archivy:
    image: archivy/archivy:latest
    container_name: archivy
    ports:
      - "5000:5000"
    volumes:
      - ./data:/archivy/data
    environment:
      - TZ=UTC
      - SECRET_KEY=${SECRET_KEY}
    restart: unless-stopped
