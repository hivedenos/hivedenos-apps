services:
  jelu:
    image: wabayang/jelu:latest
    container_name: jelu
    mem_limit: 1024m
    mem_reservation: 200m
    volumes:
      - ./database:/database
      - ./files:/files
    ports:
      - "11111:11111"
    restart: unless-stopped
