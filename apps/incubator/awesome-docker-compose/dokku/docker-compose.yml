version: 
 
services:
  dokku:
    image: dokku/dokku:0.34.8
    container_name: dokku
    network_mode: bridge
    ports:
      - 
      - 
      - 
    volumes:
      - 
      - 
    environment:
      DOKKU_HOSTNAME: dokku.me
      DOKKU_HOST_ROOT: /var/lib/dokku/home/dokku
      DOKKU_LIB_HOST_ROOT: /var/lib/dokku/var/lib/dokku
    restart: unless-stopped
