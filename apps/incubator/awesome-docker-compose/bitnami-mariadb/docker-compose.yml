version: '3.1'
 
services:
  mysql:
    image: bitnami/mysql:latest
    environment:
     MARIADB_ROOT_PASSWORD: example
    volumes:
      - /path/to/mariadb-persistence:/bitnami/mariadb/data  
    # (this is just an example, not intended to be a production configuration)
    restart: always
