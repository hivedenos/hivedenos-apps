version: '2'
services:
  thelounge:
    image: ghcr.io/thelounge/thelounge:latest
    container_name: thelounge
    ports:
      - 
    restart: always
    volumes:
      - ~/.thelounge:/var/opt/thelounge
