version: 
 
services:
  minio:
    image: quay.io/minio/minio
    command: 
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
    volumes:
      - ./minio_data:/data
    ports:
      - 9000:9000
      - 9001:9001
