services:
  dufs:
    image: sigoden/dufs
    container_name: dufs
    ports:
      - "5000:5000"
    volumes:
      - ./files:/data
    # -A allows all operations (upload, delete, etc). Remove -A for read-only.
    command: /data -A
    restart: unless-stopped
