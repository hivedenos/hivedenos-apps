services:
  websurfx:
    image: neonmmd/websurfx:latest
    ports:
      - "8080:8080"
    volumes:
      - ./config:/etc/websurfx
    depends_on:
      - redis
  redis:
    image: redis:alpine
    volumes:
      - ./redis-data:/data
