version: 
 
services:
  yaade:
    image: esperotech/yaade:latest 
    container_name: yaade
    ports:
      - 9339:9339
    environment:
      - YAADE_ADMIN_USERNAME=admin
    volumes:
      - ./yaade:/app/data
    restart: always  
