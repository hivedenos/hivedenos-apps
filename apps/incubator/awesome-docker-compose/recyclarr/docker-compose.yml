services:
  recyclarr:
    image: recyclarr/recyclarr
    container_name: recyclarr
    user: 1000:1000
    init: true
    volumes:
      - ./config:/config
    environment:
      - TZ=America/New_York
      - CRON_SCHEDULE=@daily
      - RECYCLARR_CREATE_CONFIG=true
