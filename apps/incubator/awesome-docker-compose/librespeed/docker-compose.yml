version: '3.7'
services:
  speedtest:
    container_name: speedtest
    image: ghcr.io/librespeed/speedtest:latest
    restart: always
    environment:
      MODE: standalone




      #PASSWORD:
      #EMAIL:



      #WEBPORT: 80
    ports:
      -  # webport mapping (host:container)
