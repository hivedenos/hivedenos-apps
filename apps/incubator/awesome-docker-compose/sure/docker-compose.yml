services:
  sure:
    image: ghcr.io/we-promise/sure:latest
    container_name: sure
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - SECRET_KEY=${SECRET_KEY}
    volumes:
      - ./data:/app/data
