services:
  ergo:
    image: ghcr.io/ergochat/ergo:${ERGO_VERSION:-stable}
    container_name: ergo
    restart: unless-stopped
    ports:
      - "6667:6667"
      - "6697:6697"
    volumes:
      - ./ircd.yaml:/ergo/ircd.yaml
      - ./data:/ergo/data
