services:
  recipya:
    image: reaper99/recipya:latest
    container_name: recipya
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./data:/data
    environment:
      - TZ=UTC
