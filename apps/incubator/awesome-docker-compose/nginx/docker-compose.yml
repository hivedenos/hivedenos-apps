version: '3.9'
 
services:
  web:
    image: nginx
    volumes:
      - ./templates:/etc/nginx/templates
    ports:
      - 
    environment:
      - NGINX_HOST=foobar.com
      - NGINX_PORT=80
    restart: unless-stopped
