services:
  jump:
    image: daledavies/jump:latest
    container_name: jump
    restart: unless-stopped
    ports:
      - 8080:8080
    environment:
      - TZ=UTC
    volumes:
      - ./data:/config
