services:
  mirotalk-c2c:
    image: mirotalk/c2c:latest
    container_name: mirotalk-c2c
    restart: always
    ports:
      - "${HOST_PORT}:3010"
    environment:
      - PORT=3010
      - API_KEY=${API_KEY}
