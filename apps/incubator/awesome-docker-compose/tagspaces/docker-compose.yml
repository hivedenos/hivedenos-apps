services:
  tagspaces:
    image: ghcr.io/tagspaces/tagspaces-web:latest
    container_name: tagspaces
    restart: unless-stopped
    ports:
      - "8888:80"
    volumes:
      - ./files:/usr/share/nginx/html/ext
