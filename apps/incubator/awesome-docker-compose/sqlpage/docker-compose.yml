services:
  sqlpage:
    image: lovasoa/sqlpage:latest
    container_name: sqlpage
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - ./sqlpage:/etc/sqlpage
    restart: unless-stopped
