services:
  rss-proxy:
    image: damoeb/rss-proxy:latest
    container_name: rss-proxy
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./static:/app/static
    environment:
      - NODE_ENV=production
