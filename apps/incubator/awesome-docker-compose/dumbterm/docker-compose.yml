services:
  dumbterm:
    image: dumbwareio/dumbterm:latest
    container_name: dumbterm
    restart: unless-stopped
    ports:
      - 7681:7681
    volumes:
      - ./data:/root
    environment:
      - TZ=Etc/UTC
