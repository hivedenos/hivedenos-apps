version: '2.3'
 
services:
  statping:
    container_name: statping
    image: adamboutcher/statping-ng:latest
    volumes:
      - ./statping_data:/app
    environment:
      DB_CONN: sqlite
    ports:
      - 8080:8080
    restart: always
