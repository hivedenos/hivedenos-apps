version: '3.9'
 
name: samba
 
services:
  samba:
    image: crazymax/samba
    container_name: samba
    network_mode: host
    volumes:
      - 
      - 
      - 
      - 
      - 
    environment:
      - 
      - 
    restart: always
