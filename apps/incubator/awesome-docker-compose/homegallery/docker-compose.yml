services:
  home-gallery:
    image: xemle/home-gallery:latest
    container_name: home-gallery
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
      - ./media:/media:ro
    environment:
      - TZ=UTC
