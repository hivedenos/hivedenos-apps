services:
  termix:
    image: termix/server:latest
    container_name: termix
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - TZ=UTC
      - SECRET_KEY=${SECRET_KEY}
