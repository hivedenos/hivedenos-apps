services:
  dim:
    image: ghcr.io/dusk-labs/dim:master
    container_name: dim
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./config:/opt/dim/config
      - ./media:/media
