services:
  fenrus:
    image: revenz/fenrus:latest
    container_name: fenrus
    environment:
      - TZ=UTC
    volumes:
      - ./data:/app/data
    ports:
      - 3000:3000
    restart: unless-stopped
