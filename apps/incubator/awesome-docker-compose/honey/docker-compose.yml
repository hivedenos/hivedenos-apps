services:
  honey:
    image: ghcr.io/dani3l0/honey:latest
    container_name: honey
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=UTC
    ports:
      - 3000:3000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
